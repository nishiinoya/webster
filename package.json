{
  "name": "webster",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @webster/web",
    "build": "npm run build --workspaces",
    "build:web": "npm run build -w @webster/web",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
